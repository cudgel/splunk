
# Puppet Name: <%= title %>
#
[<%= title -%>]
<%
if title == 'main'
    dbName = 'defaultdb'
else
    dbName = title
end
if @warmpath
  homePath = 'volume:splunk_warm'
else
  homePath = '$SPLUNK_DB'
end
if @coldpath
  coldPath = 'volume:splunk_cold'
else
  coldPath = '$SPLUNK_DB'
end
-%>
homePath = <%= homePath -%>/<%= dbName -%>/db
coldPath = <%= coldPath -%>/<%= dbName -%>/colddb
thawedPath = $SPLUNK_DB/<%= dbName -%>/thaweddb
frozenTimePeriodInSecs = <%= frozenTime -%>

