# Puppet Name: <%= @title %>
#
<%
selinux = scope.lookupvar('::selinux')
SPLUNK_DB = scope.lookupvar('::splunk::splunkdb')
SPLUNK_HOME = scope.lookupvar('::splunk::splunkhome')
SPLUNK_OS_USER = scope.lookupvar('::splunk::splunk_user')
SPLUNK_SERVER_NAME = scope.lookupvar('::splunk::type')
-%>
# Modify the following line to suit the location of your Splunk install.
# If unset, Splunk will use the parent of the directory this configuration
# file was found in
#
SPLUNK_HOME=<%= SPLUNK_HOME %>

#
# By default, Splunk stores its indexes under SPLUNK_HOME in the
# var/lib/splunk subdirectory.  This can be overridden
# here:
#
SPLUNK_DB=<%= SPLUNK_DB %>
<% if selinux == true %>
SPLUNK_IGNORE_SELINUX = 1
<% end %>

# Splunkd daemon name
SPLUNK_SERVER_NAME=<%= SPLUNK_SERVER_NAME %>

# Splunkweb daemon name
SPLUNK_WEB_NAME=splunkweb

SPLUNK_OS_USER=<%= SPLUNK_OS_USER %>
